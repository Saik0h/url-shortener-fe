import "./dashboard.component.css";
// import type { iDashboardProps } from "../interfaces";

export default function Dashboard() {
  // props: iDashboardProps
  // const { title, caption, columns, urls } = props

  // async function handleCopy(text: string) {
  //   await navigator.clipboard.writeText(text)
  // }

  return (

    <h1>Hello World</h1>


    // <table>
    //   <header>
    //     {title}
    //   </header>
    //   <caption>
    //     {caption}
    //   </caption>
    //   {columns.map(t => <th>{t}</th>)}
    //   <tbody>
    //     {urls.map(u => <tr><td>{u.url}</td><td>{u.acesses.length}</td> <td><button onClick={() => handleCopy(u.url)}>Copy</button></td></tr>)}
    //   </tbody>
    // </table>
  );
}
